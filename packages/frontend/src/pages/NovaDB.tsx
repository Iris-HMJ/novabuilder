import { Typography } from 'antd';

const { Title } = Typography;

const NovaDB = () => {
  return (
    <div>
      <Title level={4}>NovaDB 管理</Title>
      <div style={{ color: '#888', marginTop: 24 }}>
        (Step 5 NovaDB 正在开发中)
      </div>
    </div>
  );
};

export default NovaDB;
