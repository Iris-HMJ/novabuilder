{
  "name": "novabuilder",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "clean": "pnpm -r run clean"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "dependencies": {
    "@nestjs/config": "^4.0.3"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/pg": "^8.18.0"
  }
}
